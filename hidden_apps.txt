# Aplicaciones a esconder del launcher (nombre del .desktop en /usr/share/applications, sin la extensión)
# Se buscan en /usr/share/applications/<nombre>.desktop y se copian a ~/.local/share/applications
# con NoDisplay=true para que no salgan en el launcher, sin tocar el paquete del sistema.

#El launcher muestra el campo Name del .desktop (o su traducción Name[es] si existe), no el nombre del archivo. Para encontrar el archivo a partir de ese nombre, busca por ese campo:
#grep -rliE "^Name(\[.*\])?=NOMBRE" /usr/share/applications/*.desktop
#Por ejemplo, para kwrite:
# grep -rliE "^Name(\[.*\])?=kwrite" /usr/share/applications/*.desktop

org.kde.kwrite              # Uso kate directamente, no necesito el editor simple aparte
vim                         # No puedo desinstalarlo porque Cachy lo pide
cachyos-hello               # No lo uso nunca
cachyos-pi                  # No lo uso nunca (Instalador de paquetes de Cachy)
org.cachyos.KernelManager   # No lo uso nunca
micro                       # No puedo desinstalarlo porque Cachy lo pide
